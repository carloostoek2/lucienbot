# Phase 14 - Minijuegos (Dados y Trivia)

## Objetivo

Implementar sistema de minijuegos en Lucien Bot para generar ingresos de besitos. Dos juegos: **Dados** (victoria con pares o dobles) y **Trivia** (preguntas de docs/preguntas.json).

---

## Tasks

### Task 1: Agregar GAME y TRIVIA a TransactionSource enum

**Objective:** Extender el enum TransactionSource para soportar fuentes de transacción de minijuegos.

**Verification:** Compilación exitosa, import disponible en services.

**Files:**
- `models/models.py` - Agregar `GAME = "game"` y `TRIVIA = "trivia"` al enum TransactionSource (línea ~174)

**Action:**
```python
# En TransactionSource enum, después de ANONYMOUS_MESSAGE:
GAME = "game"           # Victoria en dados
TRIVIA = "trivia"       # Respuesta correcta en trivia
```

---

### Task 2: Crear GameService con Límites Diarios

**Objective:** Crear servicio de lógica de negocio para minijuegos (dados y trivia) con límites diarios por tipo de usuario.

**Verification:** Métodos accesibles, usa BesitoService internamente, límites diarios funcionan, VIP detection.

**Files:**
- `services/game_service.py` - Nuevo archivo

**Pattern:** DailyGiftService pattern (embeds BesitoService, usa TransactionSource, usa DailyGiftClaim para tracking)

**Límites Diarios:**
- Free users: 10 dados/día, 5 trivia/día
- VIP users: 20 dados/día, 10 trivia/día

**Estructura del servicio:**
```python
class GameService:
    """Servicio para minijuegos de dados y trivia"""

    # Constantes de límites
    DAILY_DICE_LIMIT_FREE = 10
    DAILY_DICE_LIMIT_VIP = 20
    DAILY_TRIVIA_LIMIT_FREE = 5
    DAILY_TRIVIA_LIMIT_VIP = 10

    def __init__(self, db: Session = None):
        self.db = db or SessionLocal()
        self.besito_service = BesitoService(self.db)
        self._questions = None
        self._user_service = UserService(self.db)

    # === VIP DETECTION ===
    def is_user_vip(self, user_id: int) -> bool:
        """Verifica si usuario es VIP"""
        return self._user_service.is_vip(user_id)

    def get_daily_limits(self, user_id: int) -> dict:
        """Obtiene límites diarios según tipo de usuario"""
        is_vip = self.is_user_vip(user_id)
        return {
            'dice_limit': self.DAILY_DICE_LIMIT_VIP if is_vip else self.DAILY_DICE_LIMIT_FREE,
            'trivia_limit': self.DAILY_TRIVIA_LIMIT_VIP if is_vip else self.DAILY_TRIVIA_LIMIT_FREE
        }

    # === CONTEO DIARIO ===
    def get_today_play_count(self, user_id: int, game_type: str) -> int:
        """
        Obtiene jugadas de hoy para un tipo de juego.
        Usa GameRecord con played_at en fecha actual.
        """
        today = datetime.utcnow().replace(hour=0, minute=0, second=0, microsecond=0)
        return self.db.query(GameRecord).filter(
            GameRecord.user_id == user_id,
            GameRecord.game_type == game_type,
            GameRecord.played_at >= today
        ).count()

    def can_play(self, user_id: int, game_type: str) -> tuple:
        """
        Verifica si usuario puede jugar según límites diarios.
        Returns: (puede_jugar: bool, jugadas_hoy: int, limite: int, mensaje: str)
        """
        limits = self.get_daily_limits(user_id)
        limit = limits['dice_limit'] if game_type == 'dice' else limits['trivia_limit']
        played = self.get_today_play_count(user_id, game_type)

        if played >= limit:
            is_vip = self.is_user_vip(user_id)
            if is_vip:
                return False, played, limit, "Has alcanzado tu límite diario. Regresa mañana."
            else:
                return False, played, limit, (
                    "Has alcanzado tu límite diario de juegos.\n"
                    "¡Pero! Los miembros VIP tienen el doble de oportunidades..."
                )
        return True, played, limit, None

    # === DADOS ===
    def roll_dice(self) -> tuple:
        """Lanza dos dados (1-6). Returns (dice1, dice2)"""
        import random
        return random.randint(1, 6), random.randint(1, 6)

    def check_dice_win(self, dice1: int, dice2: int) -> str:
        """
        Verifica victoria: pares (ambos pares) o dobles (iguales).
        Returns: 'pairs', 'doubles', o 'none'
        """
        if dice1 == dice2:
            return 'doubles'
        if dice1 % 2 == 0 and dice2 % 2 == 0:
            return 'pairs'
        return 'none'

    def play_dice_game(self, user_id: int) -> dict:
        """
        Procesa una partida de dados.
        Returns: {
            'dice1': int, 'dice2': int, 'won': bool,
            'win_type': str, 'besitos': int, 'message': str
        }
        """
        # 1. Verificar límites
        can_play, played, limit, limit_msg = self.can_play(user_id, 'dice')
        if not can_play:
            return {
                'dice1': None, 'dice2': None, 'won': False,
                'win_type': None, 'besitos': 0, 'message': limit_msg,
                'limit_reached': True
            }

        # 2. Roll dice
        dice1, dice2 = self.roll_dice()

        # 3. Check win
        win_type = self.check_dice_win(dice1, dice2)
        won = win_type != 'none'

        # 4. Credit besitos si ganó
        besitos = 0
        if won:
            besitos = 1  # Configurable
            self.besito_service.credit_besitos(
                user_id=user_id,
                amount=besitos,
                source=TransactionSource.GAME,
                description=f"Victoria en dados: {win_type}"
            )

        # 5. Registrar jugada
        record = GameRecord(
            user_id=user_id,
            game_type='dice',
            result=f"{dice1}+{dice2}",
            payout=besitos
        )
        self.db.add(record)
        self.db.commit()

        # 6. Construir mensaje
        if won:
            win_label = "pares" if win_type == 'pairs' else "dobles"
            message = f"🎲 {dice1} + {dice2} GANASTE!\n\n" \
                     f"¡Son {win_label}! Has ganado {besitos} besito 💋"
        else:
            message = f"🎲 {dice1} + {dice2}\n\nMejor suerte la próxima..."

        return {
            'dice1': dice1, 'dice2': dice2, 'won': won,
            'win_type': win_type, 'besitos': besitos, 'message': message,
            'limit_reached': False
        }

    # === TRIVIA ===
    def load_trivia_questions(self) -> list:
        """Carga preguntas de docs/preguntas.json"""
        import json
        from pathlib import Path

        if self._questions is not None:
            return self._questions

        questions_path = Path("docs/preguntas.json")
        if not questions_path.exists():
            logger.warning("Questions file not found: docs/preguntas.json")
            return []

        with open(questions_path, 'r', encoding='utf-8') as f:
            data = json.load(f)
            self._questions = data.get('questions', [])
        return self._questions

    def get_random_question(self) -> tuple:
        """
        Retorna pregunta aleatoria con opciones A,B,C.
        Returns: (question_dict, question_index)
        """
        questions = self.load_trivia_questions()
        if not questions:
            return None, -1

        import random
        idx = random.randint(0, len(questions) - 1)
        return questions[idx], idx

    def get_question_by_index(self, index: int) -> dict:
        """Retorna pregunta por índice"""
        questions = self.load_trivia_questions()
        if 0 <= index < len(questions):
            return questions[index]
        return None

    def check_trivia_answer(self, question: dict, answer_idx: int) -> bool:
        """Verifica si respuesta es correcta"""
        if not question:
            return False
        return question.get('answer') == answer_idx

    def play_trivia(self, user_id: int, question_idx: int, answer_idx: int) -> dict:
        """
        Procesa respuesta de trivia.
        Returns: {
            'correct': bool, 'besitos': int,
            'message': str, 'correct_answer': int
        }
        """
        # 1. Verificar límites
        can_play, played, limit, limit_msg = self.can_play(user_id, 'trivia')
        if not can_play:
            return {
                'correct': False, 'besitos': 0,
                'message': limit_msg, 'correct_answer': -1,
                'limit_reached': True
            }

        # 2. Obtener pregunta
        question = self.get_question_by_index(question_idx)
        if not question:
            return {
                'correct': False, 'besitos': 0,
                'message': "Pregunta no encontrada.", 'correct_answer': -1,
                'limit_reached': False
            }

        # 3. Verificar respuesta
        is_correct = self.check_trivia_answer(question, answer_idx)

        # 4. Credit besitos si correcto
        besitos = 0
        if is_correct:
            besitos = 2  # Configurable
            self.besito_service.credit_besitos(
                user_id=user_id,
                amount=besitos,
                source=TransactionSource.TRIVIA,
                description="Victoria en trivia"
            )

        # 5. Registrar jugada
        record = GameRecord(
            user_id=user_id,
            game_type='trivia',
            result=f"question_{question_idx}",
            payout=besitos
        )
        self.db.add(record)
        self.db.commit()

        # 6. Construir mensaje
        correct_opt = ["A", "B", "C"][question['answer']]
        if is_correct:
            message = f"🌟 ¡CORRECTO! 🌟\n\nHas respondido bien y ganado {besitos} besitos 💋"
        else:
            message = f"❌ No es correcto...\n\nLa respuesta era: {correct_opt})"

        return {
            'correct': is_correct, 'besitos': besitos,
            'message': message, 'correct_answer': question['answer'],
            'limit_reached': False
        }
```

**Recompensas:**
- Dados: 1 besito
- Trivia: 2 besitos

**Logging:** "Módulo=Acción, user_id=..., resultado=..."

---

### Task 3: Agregar GameRecord modelo

**Objective:** Crear tabla para registrar jugadas diarias.

**Verification:** Tabla existe, queries funcionan.

**Files:**
- `models/models.py` - Agregar GameRecord

**Action:**
```python
class GameRecord(Base):
    __tablename__ = "game_records"
    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, nullable=False)
    game_type = Column(String(20), nullable=False)  # 'dice', 'trivia'
    result = Column(String(50), nullable=False)
    payout = Column(Integer, default=0)
    played_at = Column(DateTime, default=datetime.utcnow)
```

---

### Task 4: Modificar inline_keyboards.py

**Objective:** Agregar keyboards de minijuegos e integrar botón en menú principal.

**Verification:** Keyboards renderizables, botón visible en main_menu_keyboard.

**Files:**
- `keyboards/inline_keyboards.py` - Agregar funciones y modificar main_menu_keyboard

**Actions:**

1. Agregar funciones de keyboards:
```python
def game_menu_keyboard() -> InlineKeyboardMarkup:
    """Menú de selección de juegos"""
    buttons = [
        [InlineKeyboardButton(text="🎲 Dados", callback_data="game_dice")],
        [InlineKeyboardButton(text="❓ Trivia", callback_data="game_trivia")],
        [InlineKeyboardButton(text="🔙 Volver", callback_data="back_to_main")]
    ]
    return InlineKeyboardMarkup(inline_keyboard=buttons)

def dice_play_keyboard() -> InlineKeyboardMarkup:
    """Botón para jugar dados"""
    buttons = [
        [InlineKeyboardButton(text="🎲 Tirar dados", callback_data="dice_play")],
        [InlineKeyboardButton(text="🔙 Menú de juegos", callback_data="game_menu")]
    ]
    return InlineKeyboardMarkup(inline_keyboard=buttons)

def trivia_keyboard(question: dict, question_idx: int) -> InlineKeyboardMarkup:
    """
    Teclado con opciones de trivia A, B, C.
    Incluye índice de pregunta en callback_data para mantener estado.
    """
    options = ["A", "B", "C"]
    buttons = []
    for idx, opt in enumerate(options):
        text = f"{opt}) {question['opts'][idx]}"
        buttons.append([InlineKeyboardButton(
            text=text,
            callback_data=f"trivia_answer_{idx}_{question_idx}"
        )])
    buttons.append([InlineKeyboardButton(
        text="🔙 Menú de juegos",
        callback_data="game_menu"
    )])
    return InlineKeyboardMarkup(inline_keyboard=buttons)
```

2. Modificar `main_menu_keyboard` (después de "📖 Fragmentos de la historia"):
```python
[InlineKeyboardButton(
    text="🎮 Minijuegos",
    callback_data="game_menu"
)]
```

---

### Task 5: Crear game_user_handlers.py

**Objective:** Crear handlers para flujos de usuario de minijuegos.

**Verification:** Handlers registrados, callbacks responden correctamente.

**Files:**
- `handlers/game_user_handlers.py` - Nuevo archivo
- `handlers/__init__.py` - Agregar import y export

**Pattern:** callback_query decorados, llamar exactamente 1 service

**Estructura de handlers:**
```python
from aiogram import Router
from aiogram.types import CallbackQuery
from keyboards.inline_keyboards import (
    game_menu_keyboard,
    dice_play_keyboard,
    trivia_keyboard
)
from services.game_service import GameService

router = Router()

@router.callback_query(lambda c: c.data == "game_menu")
async def game_menu(callback: CallbackQuery):
    """Muestra menú de minijuegos"""
    service = GameService()
    user_id = callback.from_user.id

    # Mostrar límites del usuario
    limits = service.get_daily_limits(user_id)
    is_vip = service.is_user_vip(user_id)
    vip_status = "✨ VIP" if is_vip else "Visitante"

    await callback.message.edit_text(
        text=f"🎮 <b>Minijuegos</b> — {vip_status}\n\n"
             f"Límites de hoy:\n"
             f"• Dados: {limits['dice_limit']}\n"
             f"• Trivia: {limits['trivia_limit']}\n\n"
             f"Elige tu juego:",
        reply_markup=game_menu_keyboard()
    )
    await callback.answer()

@router.callback_query(lambda c: c.data == "game_dice")
async def game_dice(callback: CallbackQuery):
    """Muestra interfaz de dados"""
    await callback.message.edit_text(
        text="🎲 <b>Juego de Dados</b>\n\n"
             "Lanza los dados y gana besitos:\n"
             "• Pares (ambos pares): +1 besito 💋\n"
             "• Dobles (iguales): +1 besito 💋",
        reply_markup=dice_play_keyboard()
    )
    await callback.answer()

@router.callback_query(lambda c: c.data == "dice_play")
async def dice_play(callback: CallbackQuery):
    """Procesa lanzamiento de dados"""
    user_id = callback.from_user.id
    service = GameService()

    result = service.play_dice_game(user_id)

    if result.get('limit_reached'):
        # Limite alcanzado - mensaje según tipo de usuario
        is_vip = service.is_user_vip(user_id)
        if is_vip:
            message = result['message']
        else:
            message = result['message'] + "\n\n" \
                     "¿Podrías ser parte del círculo íntimo? " \
                     "Los VIP tienen beneficios dobles..."
    else:
        # Resultado normal
        message = result['message']

        # Agregar contextual según victoria/derrota
        if result['won']:
            if result['win_type'] == 'pairs':
                message += "\n\n¡Los números pares te favorecen!"
            else:
                message += "\n\n¡Exactamente igual! Doble suerte."
        else:
            message += "\n\nLucien observa...下次会有 más fortuna."

    await callback.message.edit_text(message, reply_markup=dice_play_keyboard())
    await callback.answer()

@router.callback_query(lambda c: c.data == "game_trivia")
async def game_trivia(callback: CallbackQuery):
    """Inicia trivia con pregunta aleatoria"""
    service = GameService()
    user_id = callback.from_user.id

    # Verificar límites
    can_play, played, limit, limit_msg = service.can_play(user_id, 'trivia')
    if not can_play:
        is_vip = service.is_user_vip(user_id)
        if not is_vip:
            limit_msg += "\n\n¿Imaginas tener el doble de oportunidades?"
        await callback.message.edit_text(limit_msg, reply_markup=game_menu_keyboard())
        await callback.answer()
        return

    question, question_idx = service.get_random_question()

    if question is None:
        await callback.message.edit_text(
            "Las preguntas están en el taller de Lucien. Regresa más tarde.",
            reply_markup=game_menu_keyboard()
        )
        await callback.answer()
        return

    text = f"❓ <b>Trivia</b>\n\n" \
           f"Pregunta: {question['q']}\n\n" \
           f"Selecciona tu respuesta:"

    await callback.message.edit_text(
        text=text,
        reply_markup=trivia_keyboard(question, question_idx)
    )
    await callback.answer()

@router.callback_query(lambda c: c.data.startswith("trivia_answer_"))
async def trivia_answer(callback: CallbackQuery):
    """
    Procesa respuesta de trivia.
    El callback_data contiene: trivia_answer_{answer_idx}_{question_idx}
    """
    user_id = callback.from_user.id

    # Parse callback_data: trivia_answer_{answer_idx}_{question_idx}
    parts = callback.data.split("_")
    answer_idx = int(parts[2])
    question_idx = int(parts[3])

    service = GameService()
    result = service.play_trivia(user_id, question_idx, answer_idx)

    if result.get('limit_reached'):
        is_vip = service.is_user_vip(user_id)
        if not is_vip:
            result['message'] += "\n\nLos caminos de VIP siempre tienen más oportunidades..."
        await callback.message.edit_text(result['message'], reply_markup=game_menu_keyboard())
        await callback.answer()
        return

    await callback.message.edit_text(result['message'], reply_markup=game_menu_keyboard())
    await callback.answer()
```

**Voz de Lucien:**
- Elegante, 3ra persona ("Lucien observa...")
- Sarcasmo sutil en pérdidas ("Mejor suerte la próxima...")
- Sugerencias de VIP cuando límite alcanzado (free users)
- Victoria: celebración directa

---

### Task 6: Registrar handlers en bot.py

**Objective:** Integrar handlers de minijuegos al pipeline del bot.

**Verification:** Bot inicia sin errores, callbacks de juegos responden.

**Files:**
- `handlers/__init__.py` - Agregar import
- `bot.py` - Agregar import y register

**Actions:**

1. En `handlers/__init__.py`:
```python
# Phase 14 - Minijuegos
from .game_user_handlers import router as game_user_router

__all__ = [
    # ... existing ...
    # Phase 14 - Minijuegos
    'game_user_router'
]
```

2. En `bot.py`:
```python
# Phase 14 - Minijuegos
from handlers import game_user_router

# En include_routers:
dp.include_router(game_user_router)
```

---

### Task 7: Crear migración Alembic

**Objective:** Crear tabla game_records en base de datos.

**Verification:** Tabla creada, queries funcionan.

**Files:**
- `alembic/versions/` - Nuevo archivo de migración

**Action:**
```bash
alembic revision --autogenerate -m "Add game_records table"
alembic upgrade head
```

---

## Verification Checklist

- [x] TransactionSource enum tiene GAME y TRIVIA
- [x] GameRecord modelo existe en models
- [x] GameService con límites diarios funciona
- [x] VIP detection para límites funciona
- [x] keyboards/inline_keyboards.py tiene game_menu_keyboard, dice_play_keyboard, trivia_keyboard
- [x] main_menu_keyboard tiene botón "🎮 Minijuegos"
- [x] handlers/game_user_handlers.py con voz de Lucien
- [x] handlers/__init__.py exporta game_user_router
- [x] bot.py incluye game_user_router
- [x] Migración create game_records ejecutada
- [x] Bot inicia sin errores de importación

---

## Mensajes de Voz de Lucien

### Dados - Victoria
```
🎲 2 + 2 GANASTE!
¡Son pares! Has ganado 1 besito 💋
Los números pares te favorecen!
```

### Dados - Derrota
```
🎲 3 + 5
Mejor suerte la próxima...
Lucien observa...下次会有 más fortuna.
```

### Dados - Límite alcanzado (Free)
```
Has alcanzado tu límite diario de juegos.
¿Podrías ser parte del círculo íntimo? Los VIP tienen beneficios dobles...
```

### Dados - Límite alcanzado (VIP)
```
Has alcanzado tu límite diario de juegos. Regresa mañana.
```

### Trivia - Correcto
```
🌟 ¡CORRECTO! 🌟
Has respondido bien y ganado 2 besitos 💋
```

### Trivia - Incorrecto
```
❌ No es correcto...
La respuesta era: B)
```

---

## Success Criteria

1. Usuario puede acessar menú de minijuegos desde menú principal
2. Dados: usuario tira, ve resultado "X + Y", besito si gana (pares o dobles)
3. Trivia: pregunta aleatoria de preguntas.json, besitos por respuesta correcta
4. Límites diarios según tipo de usuario (free 10/5, VIP 20/10)
5. Mensajes con voz de Lucien (elegante, sarcasmo sutil)
6. Sugerencia VIP cuando límite alcanzado (free users)
7. Sin lógica de negocio en handlers
8. Transacciones registradas con TransactionSource correcto